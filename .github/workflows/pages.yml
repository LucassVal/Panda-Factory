name: 🐼 GitHub Pages Deploy (JAM React)

on:
  push:
    branches: [main]
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🐼 Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: "11.jam/package-lock.json"

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: 📦 Install Dependencies
        working-directory: 11.jam
        run: npm ci

      - name: 🔨 Build JAM React App
        working-directory: 11.jam
        run: npm run build

      - name: 📋 Copy Root Assets to Dist
        run: |
          # Copy manifest and PWA assets to dist
          cp manifest.json 11.jam/../dist/jam/ 2>/dev/null || true
          cp favicon.ico 11.jam/../dist/jam/ 2>/dev/null || true
          echo "✅ Assets copied to dist/jam"

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/jam

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: 🐼 Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: 🎉 Deployment Complete
        run: |
          echo "🐼 Panda Factory JAM UI v6.4 is LIVE!"
          echo "URL: ${{ steps.deployment.outputs.page_url }}"
